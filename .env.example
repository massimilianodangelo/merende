# Dati di configurazione per l'applicazione

# URL del database (obbligatorio per la connessione a Neon Database)
DATABASE_URL=postgresql://user:password@db.example.neon.tech/dbname?sslmode=require

# Chiave segreta per le sessioni (modifica con una stringa casuale)
SESSION_SECRET=una-chiave-molto-segreta-da-cambiare-assolutamente

# Ambiente (development o production)
NODE_ENV=development
